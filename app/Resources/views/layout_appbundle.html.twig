{% extends "::Layout.html.twig" %}

{% block stylesheet %}
    {{ parent() }}
    <link rel="stylesheet" href="css/style.css" />
    {#<style>#}

        {#article img{#}
            {#max-width:280px;#}
            {#width:100%;#}
            {#float:left;#}
            {#margin-right:10px#}
        {#}#}
    {#</style>#}
{%endblock%}








{% block body %}

    {#<div class="col-sm-3">#}
        {#{% block menu %}#}
            {#            {{ render(controller("AppBundle:Blog:menuGauche", {'nombre':3})) }}#}
            {#<div class="navbar">#}
                {#<div class="navbar-inner">#}

                    {#{{ knp_menu_render('AppBundle:Builder:mainMenu', {'currentClass': 'active'}) }}#}
                {#</div>#}
            {#</div>#}

        {#{% endblock %}#}

    {#</div>#}

    <div class="navbar navbar-default navbar-fixed-top" style="z-index: 1000; padding: 8px 0" >
        <div  class="filters "  >
            <div class="col-md-6">
                {#<h3 class="hidden-xs hidden-sm">Famille</h3>#}
                {#<span class="hidden-xs hidden-sm" style="text-align: center;display: block;">Famille</span>#}
                <div class="btn-group btn-group-justified" data-toggle="buttons" data-filter-group="famille">
                    {% for famille in familles %}
                        <label type="button" class="btn btn-default" data-filter=".{{ famille.nom}}">
                            <span class="hidden-xs {{ famille.icon }}"></span>
                            <input type="radio" name="options" id="option2" autocomplete="off"> <span class="">{{ famille.nom }}</span>
                        </label>
                    {% endfor %}
                </div>
            </div>
            <div class="col-md-6">
                {#<h3 class="hidden-xs hidden-sm">Categories</h3>#}
                {#<span class="hidden-xs hidden-sm" style="text-align: center;display: block;">Categories</span>#}
                <div class="btn-group btn-group-justified" data-toggle="buttons" data-filter-group="categorie">
                    {% for categorie in categories %}
                        <label class="btn btn-default" data-filter=".{{ categorie.nom }}">
                            <span class="hidden-xs {{ categorie.icon }}"></span>
                            <input type="radio" name="options" id="option2" autocomplete="off"> {{ categorie.nom }}
                        </label>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-12">
        {% for message in app.session.flashbag.get('info') %}
            <p class="alert alert-success">{{message}}</p>
        {%endfor%}
        {% for message in app.session.flashbag.get('erreur') %}
            <p class="alert alert-danger">{{message}}</p>
        {%endfor%}
        {% block contenu %}

        {%endblock%}
    </div>


{%endblock%}

